# Big picture

## Wishlist

+ [ ] FastAPI: routing, request/response schemas, validation, error handling.
+ [ ] Adapter for model providers
+ [ ] Observer for logging/metrics hooks.
+ [ ] `.env`, structured logs, consistent error responses.
+ [ ] PyTest with mocked LLM client; snapshot/golden tests for prompt stability.
+ [ ] `pyproject.toml`, local package layout
+ [ ] GitHub Actions (lint + tests).
+ [ ] Dockerfile and run with environment variables.

## Endpoints

### Prompts

+ `POST /v1/prompts` – create prompt

  + body: `{ purpose: str, name: str, template: str }`
  + returns: `{ id, purpose, name, template, version, active }`
+ `GET /v1/prompts?purpose=...` – list prompts (filter by purpose)
+ `PATCH /v1/prompts/{id}` – modify `name`/`template` (bumps `version`)
+ `POST /v1/prompts/{id}/activate` – set as active for `(user, purpose)`
+ `GET /v1/prompts/active?purpose=...` – fetch active prompt for purpose

### Prediction

+ `POST /v1/predict`

  + body: `{ purpose: str, document_text: str, params?: {max_tokens?: int, temperature?: float}, provider?: "mock"|"openai"|"gemini"|"watson" }`
  + behavior: load active prompt for purpose → render → call provider client
  + returns: `{ output_text, model_info: {...}, prompt_id, prompt_version, latency_ms }`

**possible simplification**: single “current user” context read from `X-User-Id` header or default `user_anon`.

### Week 2 Additions

+ `/health/db` check using a quick `SELECT 1`.
+ `GET /v1/history?limit=50&purpose=&user_id=` – returns recent predictions with
  timestamp, prompt_id, user_id, latency, provider/model.
+ (Optional) `POST /v1/prompts/export` – triggers CSV export of prompt usage
  logs to `/var/exports/prompt_logs.csv`.
+ Existing endpoints continue to work, now backed by the DB (with the legacy
  store available behind a feature flag).

### Week 3 Additions: vector database

+ `POST /v1/chunks/insert` receives a chunk (under a maximum length) and inserts
  into a vector database. Responds with an HTTP status code indicating if the
  insertion was successful or not.
+ `GET /v1/chunks/retrieve?text=...&n=...` - queries the vector database and
  returns the `n` chunks nearest to `text`.
+ Services for insertion and retrieval of chunks
+ Service for chunking a text
+ Prompt expansion based on Jinja2
+ Tests for all these features

## Folder structure

```text
prompted-doc-processor/
├─ app/
│  ├─ services/
│  │  ├─ db.py                 # SQLModel/SQLAlchemy engine + session helpers (async-friendly)
│  │  ├─ vectordb.py           # Abstract class and ChromaDB implementation
│  │  ├─ prompt_store_sql.py   # DB-backed PromptStore implementation
│  │  ├─ prompt_store.py       # in-memory/file snapshot store kept for comparison
│  │  ├─ processor.py          # writes PromptUsage logs to DB
│  │  └─ llm_client.py
│  ├─ api/
│  │  ├─ routes_history.py     # `/v1/history` or `/v1/logs` endpoint
│  │  ├─ routes_prompts.py     # uses PromptStoreSQL by default
│  │  ├─ routes_chunks.py      # 
│  │  └─ routes_predict.py     # imports DB logging from processor
│  ├─ models/
│  │  ├─ tables.py             # SQLModel table definitions (Prompt, PromptUsage)
│  │  ├─ schemas.py            # request/response Pydantic schemas
│  │  └─ domain.py             # align with DB entities, add helpers
│  └─ core/
│     └─ config.py             # DATABASE_URL, ASYNC_DB, alembic env vars
├─ migrations/                 # Alembic setup (env.py, versions/)
│  ├─ env.py
│  └─ versions/
│     └─ xxxx_init.py
├─ tests/
│  ├─ fixtures/
│  │  ├─ db_fixture.py         # create/drop test DB, seed prompts
│  │  └─ __init__.py
│  ├─ test_db_store.py         # CRUD + activation + versioning tests
│  ├─ test_history_api.py      # history endpoint tests
│  ├─ test_predict_api.py      # asserts usage logs created
│  └─ conftest.py              # includes DB session fixture
└─ var/
   └─ exports/
      └─ prompt_logs.csv       # Generated by ETL-lite export
```
